import { pad, secondsToTimeObj, secondsToTimeStr } from '../src/util';


describe('pad', () => {
  it.skip('should ...', () => {
    console.log(pad);
  });
});


describe('secondsToTimeObj', () => {
  it.skip('should ...', () => {
    console.log(secondsToTimeObj);
  });
});

describe('secondsToTimeStr', () => {
  it.skip('should ...', () => {
    console.log(secondsToTimeStr);
  });
});
